Use the gap-analysis skill to produce a gap analysis.

If a document was provided, read it first. If the baseline and target state are not clear from context, ask the user to describe the current state and target state before proceeding.
